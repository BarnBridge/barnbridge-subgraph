{
  "name": "barnbridge",
  "version": "0.1.0",
  "scripts": {
    "create": "graph create BarnBridge/barnbridge-subgraph --node https://api.thegraph.com/deploy/",
    "create-local": "graph create BarnBridge/barnbridge-subgraph --node http://127.0.0.1:8020",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy BarnBridge/barnbridge-subgraph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy BarnBridge/barnbridge-subgraph --ipfs http://localhost:5001 --node http://127.0.0.1:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.1"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
